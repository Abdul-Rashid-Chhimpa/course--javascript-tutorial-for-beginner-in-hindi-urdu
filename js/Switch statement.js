
      var age = 28;
      switch (true) {
        case (age >= 18 && age <= 30):
         document.write("you are eligible to for voting");
          break;
          case (age >= 15 && age <= 18):
           document.write("you are not eligible to for voting");
            break;

        default:
        document.write("Please enter a vaild age");

      }
  
