
         // global variable.
         // global variable as work inside and outside of the function.
           var b = "abdul";
      function Myvalue()
      {
                        /* Local variable.
                         local variable only work inside of function.
                         local variable declare outside of function as given error. */
        document.write(b);
      }
      Myvalue();
